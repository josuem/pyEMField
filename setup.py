from setuptools import setup, find_packages

setup(
    name='pyEMField',
    version='0.1',
    packages=find_packages(),
    # install_requires=[],
    author='Josue Meneses Díaz',
    author_email='josue.meneses+pyEMField@usach.cl',
    description='A library to automate calculations related to electromagnetics in Python',
    url='https://github.com/josuem/pyEMField',
)
